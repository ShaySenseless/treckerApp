<%= form_for(@leistung) do |f| %>
  <% if @leistung.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leistung.errors.count, "error") %> prohibited this leistung from being saved:</h2>

      <ul>
      <% @leistung.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
